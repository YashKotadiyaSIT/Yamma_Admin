import { VerificationStatusPipePipe } from './verification-status-pipe.pipe';

describe('VerificationStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new VerificationStatusPipePipe();
    expect(pipe).toBeTruthy();
  });
});
